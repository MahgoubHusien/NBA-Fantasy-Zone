version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - cd frontend
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: frontend/.next
        files:
          - '**/*'
      cache:
        paths:
          - frontend/node_modules/**/*
    backend:
      phases:
        preBuild:
          commands:
            - cd nba-zone
            - ./mvnw install
        build:
          commands:
            - ./mvnw package
      artifacts:
        baseDirectory: nba-zone/target
        files:
          - '*.jar'
